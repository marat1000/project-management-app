export const enum ELSKeys {
  token = 'TOKEN',
}

export enum ERoutes {
  singIn = '/singin',
  singUp = '/signup',
  main = '/main',
  welcome = '/welcome',
  profile = '/profile',
  boards = '/boards',
}
